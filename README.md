### To run the project locally, you should clone it then open two terminals :

In the first one run :
```bash
npm install
npm run dev
```

In the second one run :
```bash
npx json-server --watch src/db.json --port 3000
```
